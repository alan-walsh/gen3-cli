# gen3-cli
An agentic CLI for the Gen3 platform.

## Skills scaffold

An initial `skills/` scaffold is in place for the future Rust + Ratatui Gen3 CLI.

It follows the same broad skill pattern as `gws`: a shared skill plus one directory per service with a `SKILL.md` file describing purpose, command shape, and the initial API areas to grow.
